services:
  postgres:
    image: postgres:16-alpine
    container_name: vaultgame-postgres
    environment:
      POSTGRES_USER: vaultgame
      POSTGRES_PASSWORD: vaultgame
      POSTGRES_DB: vaultgame
    ports:
      - "5433:5432"
